# Fastify server
PORT=8080

# Supabase (required)
# Copy from Supabase Dashboard → Settings → API
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Supabase service key (recommended on server for admin ops)
# Copy from the same page; keeps writes reliable even if client token missing
SUPABASE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY

# Optional admin list (comma-separated Supabase user IDs)
ADMIN_USER_IDS=

# Optional: Upstash Redis (used opportunistically, safe to leave empty)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Optional: Twilio SMS (used opportunistically)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM=

# Notes
# - Do NOT wrap values in quotes.
# - Ensure there are no trailing spaces.
# - Restart the server after changes.
