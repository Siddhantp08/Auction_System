# Client → Server API origin
# For local dev, keep this as your server URL
VITE_API_BASE=http://localhost:8080

# WebSocket URL (matches server PORT and protocol)
VITE_WS_URL=ws://localhost:8080

# Optional fallback Supabase config (client usually pulls from /config)
# If /config isn’t returning values, set these temporarily for testing.
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Notes
# - Do NOT wrap values in quotes.
# - Restart the Vite dev server after changes.
